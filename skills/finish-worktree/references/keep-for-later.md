# Keep for Later

Preserve the completed task state without integrating, publishing, or cleaning it up.

1. Make no branch, checkout, index, worktree, filesystem, or remote mutation.
2. Reconfirm the task branch and worktree still match the recorded clean, verified state.
3. Report the base branch and commit, task branch and commit range, worktree path, creation owner,
   verification result, publication state, and the remaining integration or cleanup owner.
