interface:
  display_name: "Finish Worktree"
  short_description: "Finalize a worktree through a closed mode"
  default_prompt: "Use $finish-worktree to validate and execute this worktree's finalization contract."
